spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shoddon/spring-cloud/tree/master/root
          username:
          password:
      label: master

server:
  port: 5555
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7777/eureka/


